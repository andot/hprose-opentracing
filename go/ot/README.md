# OpenTracing support for hprose in Go

The `ot` package makes it easy to add OpenTracing support to hprose-based
systems in Go.

## Installation

```
go get github.com/hprose/hprose-opentracing/go/ot
```

